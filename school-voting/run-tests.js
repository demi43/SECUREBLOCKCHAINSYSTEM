import { config } from "hardhat/config";
import "./hardhat.config.js";
import { run } from "mocha";

// This will be handled by mocha when we run it
// We just need to ensure hardhat is loaded first

